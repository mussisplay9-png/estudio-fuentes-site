# Contact form submission endpoint (POST, multipart/form-data, Accept: application/json).
# Works as-is with a Formspree ("https://formspree.io/f/xxxxxxx") or Web3Forms endpoint.
# Until this is set, the form falls back to opening a pre-filled mailto: link.
PUBLIC_FORM_ENDPOINT=
